= semantic_form_for @workspace do |f|
  = render :partial => "globale", :object => @workspaces
  = f.inputs do
    = f.input :name
    = f.input :mail 
    = f.input :phone
    
  = f.buttons do
    = f.commit_button
