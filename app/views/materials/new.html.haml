-# encoding: utf-8 
-content_for :left_top do
  .left_submenu
    %h2.grey
      Menu
    %ul
      %li.LM_pic_liaison
        =link_to 'Retour au laboratoire', :controller => :laboratory, :action => :index, :anchor => 'Matériaux'
    
-content_for :workspace_content do
  .grey
    - if @material.mtype.nil?
      /- @behaviors = ["Elastique", "Elastique Plastique", "Elastique Endomageable", "Elastique Plastique Endomageable"]
      /- @types     = ["Isotrope", "Orthotrope"]
      - @types     = ["Isotrope Elastique", "Isotrope Elastique Plastique", "Isotrope Elastique Endomageable", "Orthotrope Elastique", "Mésomodèle"]

      = form_for new_workspace_material_path, :html => {:method => :get} do 
        %p
          = label_tag("Type")
          = select "", :type, @types
        %p
          = submit_tag "Suivant", :class => 'yellow_button'
    - else
      = render "form"
