-content_for :left_top do
  .left_submenu
    %h2.grey
      Comptes
    %ul
      %li.manage_pic_nouveau_membre
        = link_to "Ajouter un membre", new_company_member_path (current_user.company)


-content_for :right_top do
  %h2.grey
    Liste des membres
  = render :partial => "search_filter"

-content_for :company_content do
  %table.grey
    %thead
      %tr
        %th
        %th                  Mail
        %th                  Nom
        %th                  Role
        %th{ :colspan => 2 } Status
    %tbody
      = render :partial => "member", :collection => @members


