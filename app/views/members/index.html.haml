-content_for :left_top do
  .left_submenu
    %h2.grey
      Comptes
    %ul
      %li.manage_pic_nouveau_membre
        = link_to "Ajouter un membre", new_company_member_path (current_company_member.company)


-content_for :right_top do
  %h2.grey
    Liste des membres
  = render :partial => "search_filter"

-content_for :company_content do
  = render :partial => "members_list", :object => @members

