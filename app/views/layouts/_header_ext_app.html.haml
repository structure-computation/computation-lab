/ partie header commune à toute les pages, CSproject.selected
#headerLarge{:style => "position:fixed; top:0;"}
  #conteneur
    .content
      #header
        #bl_1.black.off
        #header_top_logo
        #header_top_cadena
        #header_top_user
          - if user_signed_in?
            = current_user.full_name
            %br
            = link_to "déconnexion", destroy_user_session_path,  :id => "disconnect_link"
            
        #header_top_temp
        #header_top_societe
          - if user_signed_in? && current_user.company
            = current_user.company.name
            / TODO: Supprimer la ligne suivante dans une version ultérieure. C'est une mauvaise façon de se connecter.
            - if(current_user.company.id == 240607020)
              %br
              %a{:href => "/sc_admin_company", :id=>"administration_link" }
                administration

/ fin partie header
